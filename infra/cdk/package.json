{
  "name": "weather-warning-cdk",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "synth": "npm run build && cdk synth",
    "diff": "npm run build && cdk diff",
    "deploy": "npm run build && cdk deploy",
    "import": "npm run build && cdk import"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.180.0",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "aws-cdk": "^2.180.0",
    "typescript": "^5.4.0"
  },
  "overrides": {
    "fast-uri": "^3.1.2",
    "brace-expansion": "^5.0.6"
  }
}
